<UserControl x:Class="FitnessAppWPF.Controls.WorkoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessAppWPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50"
             d:DesignWidth="600" BorderBrush="Black" BorderThickness="1" Margin="1">
    <Grid Background="#48a6ee">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="#000000" Margin="2,2,1,2" Grid.Column="0">
            <TextBlock Text="{Binding Name}" Foreground="#48a6ee" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5"/>
        </StackPanel>
        <StackPanel Background="#000000" Margin="1,2,2,2" Grid.Column="1">
            <TextBlock Text="{Binding Description}" Foreground="#48a6ee" FontSize="10" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5"/>
        </StackPanel>

    </Grid>



</UserControl>
