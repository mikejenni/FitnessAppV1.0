<UserControl x:Class="FitnessAppWPF.Controls.WorkoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FitnessAppWPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50"
             d:DesignWidth="600" BorderBrush="Black" BorderThickness="0,1,0,0">
    <Grid Background="#CED8F6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5"/>
        <TextBlock Grid.Column="1" Text="{Binding Description}" FontSize="10" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5"/>

    </Grid>



</UserControl>
